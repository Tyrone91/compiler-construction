Start := StatementList EOF

STATEMENT_LIST := STATEMENT STATEMENT_LIST'
STATEMENT_LIST' := STATEMENT_LIST | €

WHILE:= while (EXPRESSION) STATEMENT

IF:= if(EXPRESSION) STATEMENT else STATEMENT

STATEMENT := WHILE | { STATEMENT_LIST } | DECLARE | ASSIGNMENT | PRINT | IF

DECLARE := var IDENTIFER DECLARE';
DECLARE' := € | = EXPRESSION

PRINT := print EXPRESSION;

ASSIGNMENT := IDENTIFER = EXPRESSSION;

---------------- arithmetic ----------------

EXPRESSION :=  THERME EXPRESSION'
EXPRESSION' := + THERME EXPRESSION' | - THERME EXPRESSION' | €

THERME := FACTOR THERME'
THERME' := * FACTOR THERME' | / FACTOR THERME' | €

FACTOR := NUMBER | IDENTIFER | -FACTOR | (EXPRESSION) | IS_NEG

IS_NEG:= isNeg(EXPRESSION)